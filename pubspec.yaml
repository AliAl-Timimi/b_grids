name: b_grids
description: "A new Flutter project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: ">= 1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
